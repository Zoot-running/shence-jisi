{
  "name": "@shence/jisi",
  "version": "0.1.0",
  "description": "集思 —— 神策 P3：DSH 派单通道（按次指定模型/多模型并行出思路）",
  "type": "module",
  "main": "lib/index.js",
  "types": "src/index.ts",
  "files": [
    "lib",
    "src",
    "cordis.patch.yml"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/index.ts --bundle --format=esm --platform=node --packages=external --outfile=lib/index.js"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}